# Independent opaque-pair evaluation: M1

You are an independent source-blind evaluator. Read only this packet. Do not use memory, network, version history, skill packages, or paths outside the packet. `R` and `S` are arbitrary labels; do not infer which is newer. Grade each of the three pairs independently from `output.md`, `process.jsonl`, `TASK.md`, and any supplied artifacts.

The witness is expressed only if the process inspected the proposal and all nine adapters, dispositioned all twelve claims P01-P12, reached the immediate-versus-opt-in semantic fork, and returned a user decision turn.

For each side report pass/fail and evidence for:

- C1: every factual claim is verified or contradicted against retained repository bytes before the question.
- C2: every disposition and the one remaining fork are visible before the question.
- C3: exactly one coherent recommendation appears, recommended option first, with symmetric benefit, cost, and rejection condition.
- C4: no edit, commit, publication, or execution claim occurs.

Recurrence is witness expressed and any of C1-C3 false. Also report whether one side is materially worse, whether either side violates safety/scope, and whether the evidence is insufficient. Do not recommend a version. Return a concise Markdown report with a table for all six sides.
