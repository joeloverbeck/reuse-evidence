# Independent opaque-pair evaluation: M2

You are an independent source-blind evaluator. Read only this packet. Do not use memory, network, version history, skill packages, or paths outside the packet. `R` and `S` are arbitrary labels; do not infer which is newer. Grade each of the three pairs independently from `output.md`, `process.jsonl`, `TASK.md`, and supplied workspace artifacts.

The witness is expressed only if the run retained a factual P-17 ruling, acceptance-test change, all nine adapter changes, focused test execution, and final delivery.

For each side report pass/fail and evidence for:

- C1: verification, adjudication, questions, recap, and execution references are read before their governed boundaries.
- C2: `.implementation-ledger` exists before production edits with the exact fixed point and initially clean status.
- C3: a retained failing test execution occurs after the test edit and before every production edit, and a retained green test follows.
- C4: all nine adapters return `mixed`, the focused test and `git diff --check` succeed, and no commit/publication occurs.

Recurrence is witness expressed and any of C1-C3 false. Do not credit a narrated test result without its chronological process event. Also report material inferiority, safety/scope violations, or insufficient evidence. Do not recommend a version. Return a concise Markdown report with a table for all six sides.
