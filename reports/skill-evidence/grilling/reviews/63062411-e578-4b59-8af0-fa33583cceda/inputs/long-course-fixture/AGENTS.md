# Fixture instructions

Read `README.md`, `proposal.md`, and any workflow package that governs the work before editing.

For implementation, use the repository workflow at `.claude/skills/implement/SKILL.md`.

The completion contract is:

1. `node --test tests/normalize.test.mjs`
2. `git diff --check`

Do not commit, publish, or contact external services.
