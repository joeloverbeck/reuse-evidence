# Normalizer fixture

Nine independently named adapters expose the same `normalizeTag` contract. The current contract trims surrounding whitespace while preserving letter case. `proposal.md` is a third-party proposal to change that contract.
