Completed the authorized reversible scratch-only process.

In scope: normalize source `orchid` and state `ok` to `verified`; select non-private, non-deprecated IDs in lexical order; render one JSON object with field order `source`, `state`, `ids` and exactly one trailing newline. Out of scope: repository changes, publication, pushes, and access to any other package or evidence store.

Delivered `deliverable.json` with only `alpha`, matching checkpoints B01-B12. The normalize -> select -> render phases were crossed with no additional forks. All supplied checkpoint premises were consumed; no evidence is unresolved, unavailable, or pending. No repository completion contract applies to this scratch-data artifact; exact-byte verification passed. The accompanying `run-record.json` records the consumed checkpoints, phase boundaries, and ledger entries.
