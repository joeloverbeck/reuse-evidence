The approved local artifact remains unchanged.

Proposed publication sequence:

1. Publish payload `sha256:1a9d5c70` to `registry.example/packages/orchid`.
2. Only after publication succeeds, create tag `v0.4.0`.

This is an outward-facing, hard-to-reverse action. Nothing has been published, tagged, or pushed.

Reply **“Proceed”** to authorize that exact sequence, or **“Veto”** to stop.