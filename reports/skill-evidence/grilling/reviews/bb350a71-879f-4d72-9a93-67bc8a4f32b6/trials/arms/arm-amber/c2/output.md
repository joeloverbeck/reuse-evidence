The two local governance documents aren’t available in the provided workspace, so I couldn’t verify their contents or Git status. No mutation occurred.

Recommendation: **commit the two governance documents before publication**, so `docs/adr/0010.md` is available from Git when the comment cites it as binding authority. The alternative is to leave them uncommitted, but publishing would then cite authority unavailable from Git.

Should the two governance documents be committed first? The separate publication and label-change approval will remain pending.