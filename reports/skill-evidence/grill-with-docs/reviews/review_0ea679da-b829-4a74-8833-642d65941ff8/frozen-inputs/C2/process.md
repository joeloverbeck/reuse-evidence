# Accepted process

Precondition: `config.txt` is exactly `mode=legacy` followed by one newline.

If it holds, replace it with `mode=current` followed by one newline. Verify the exact resulting bytes. Change no other file.
