# Export plan

The export must preserve immutable snapshots. Choose one unresolved storage strategy:

1. one local file per snapshot; or
2. one append-only local stream for all snapshots.

After that choice, choose whether export names are human-readable or opaque. Nothing is ratified yet.
